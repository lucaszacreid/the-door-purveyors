# Postgres connection string (required for Vercel production)
# Get a free database at https://neon.tech — copy the connection string from the dashboard
DATABASE_URL=postgresql://user:password@hostname/dbname?sslmode=require
